import { CreateServer } from './createServer';
import { Middleware } from './createRoute';
export declare type CreateTestOptions = {
    middleware?: Middleware[];
};
export declare type CreateTest = CreateServer;
