import { Middleware } from './createRoute';
import { ServerfullRoute } from './routeHandler';
export declare type CreateRouterOptions = {
    middleware?: Middleware[];
};
export declare type ImportedRoute = {
    path: string;
    route: ServerfullRoute;
    file: string;
};
