import { HandlerFunction } from '@lightjs/types';
import { IncomingMessage, ServerResponse } from 'http';
export declare function convertHandlerFunctionToRequestHandler(handler: HandlerFunction): (req: IncomingMessage, res: ServerResponse) => any;
