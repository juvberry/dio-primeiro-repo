import { CommandBuilder } from 'yargs';
export declare const command = "start";
export declare const desc = "start a production server";
export declare const builder: CommandBuilder;
interface Args {
    port?: string;
}
export declare const handler: (argv: Args) => void;
export {};
