import Router from 'find-my-way';
import { ImportedRoute, Middleware } from '@lightjs/types';
declare type InjectRoutesOptions = {
    middleware?: Middleware[];
};
export declare function injectRouteIntoRouter(router: Router.Instance<Router.HTTPVersion.V1>, routes: ImportedRoute[], opts: InjectRoutesOptions): void;
export {};
