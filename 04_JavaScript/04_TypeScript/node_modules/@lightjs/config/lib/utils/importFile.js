"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.importTSorJSFile = void 0;
const fs_1 = require("fs");
function importFile(fileName) {
    if ((0, fs_1.existsSync)(fileName)) {
        let conf = require(fileName); // eslint-disable-line
        if (!conf)
            return null;
        if (conf.default)
            conf = conf.default;
        return conf;
    }
    return null;
}
function importTSorJSFile(fileName) {
    return importFile(`${fileName}.ts`) || importFile(`${fileName}.js`);
}
exports.importTSorJSFile = importTSorJSFile;
