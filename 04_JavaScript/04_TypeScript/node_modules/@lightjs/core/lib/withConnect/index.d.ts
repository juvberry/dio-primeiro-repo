import { Context } from '@lightjs/types';
import type { NextHandleFunction } from 'connect';
export declare const withConnect: (connect: NextHandleFunction) => (fn: any) => (ctx: Context) => Promise<any>;
